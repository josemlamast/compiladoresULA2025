fun calcularPromedio(x) 
    if(length(x) == 1)
        (head(x))
    else
      (head(x)) + calcularPromedio(tail(x))
    end
end

fun calcularAreaCuadrado(x) 
    print (x)
end

fun calcularAreaCTiangulo(x) 
        print (x)
end



let w = [1 ,2 , 3] in
    if (length(w) > 2)
         let y = tail(w) in calcularAreaCTiangulo ((head(z) , head((tail(z)))))   end
    else
            let z = tail(w) in calcularAreaCTiangulo(head((tail(z))) , head(z))   end
    end
    
end

   